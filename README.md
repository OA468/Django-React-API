# Dockerized-Django-React-API

## Usage
Clone the repository:
```bash
git clone https://github.com/OA468/Django-React-API.git
```

Navigate into the project directory:
```bash
cd Django-React-API.git
```

 Start the Docker containers:
```bash
docker-compose up
```
